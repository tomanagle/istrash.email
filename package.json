{
  "name": "istrashemail",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "fetch-domains": "bun scripts/fetch-domains.ts",
    "prebuild": "bun scripts/fetch-domains.ts",
    "deploy": "bun run prebuild && wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.12.4",
    "@types/node": "^25.6.0",
    "typescript": "^5.5.2",
    "vitest": "~3.2.0",
    "wrangler": "^4.81.1"
  }
}